# EasyCampus（易校）本地部署运行指南

## 一、环境要求

| 依赖 | 版本要求 | 说明 |
|------|---------|------|
| JDK | 17 或更高 | 后端运行环境 |
| Maven | 3.6+ | 后端构建工具（项目自带 mvnw，可不单独安装） |
| Node.js | 18 或更高 | 前端运行环境 |
| npm | 9 或更高 | 前端包管理器 |
| MySQL | 8.0 | 数据库 |

---

## 二、项目结构

```
EasyCampus-main/
├── backend/                    # Spring Boot 后端
│   ├── src/main/java/          # Java 源代码
│   ├── src/main/resources/     # 配置文件 + SQL 脚本
│   ├── pom.xml                 # Maven 依赖配置
│   └── mvnw / mvnw.cmd         # Maven Wrapper（Linux/Mac、Windows）
├── frontend/                   # Vue 3 前端
│   ├── src/                    # 前端源代码
│   ├── package.json            # npm 依赖配置
│   └── vite.config.ts          # Vite 构建配置
└── nginx/                      # 生产环境 Nginx 配置
```

---

## 三、数据库配置

**推荐方式**：使用 `dev,init` profile 启动后端，自动完成建库、建表和演示数据写入（见第四节），无需手动执行 SQL。

**手动方式**（如需单独初始化）：

```sql
-- 登录 MySQL 后执行
CREATE DATABASE IF NOT EXISTS campus_market_dev
  DEFAULT CHARACTER SET utf8mb4
  DEFAULT COLLATE utf8mb4_unicode_ci;
```

```bash
mysql -u root -p campus_market_dev < backend/src/main/resources/schema-mysql.sql
mysql -u root -p campus_market_dev < backend/src/main/resources/data-mysql.sql
```

> **密码配置**：后端通过环境变量 `DB_PASSWORD` 读取数据库密码（见 4.1 节），**请勿把真实密码写入配置文件**。

---

## 四、后端启动

### 4.1 配置说明

开发环境配置文件：`backend/src/main/resources/application-dev.yml`

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| 数据库地址 | `localhost:3306/campus_market_dev` | 库不存在时自动创建 |
| 数据库用户 | `root` | 本地开发默认账号 |
| 数据库密码 | 环境变量 `DB_PASSWORD` | 未设置时回退 `123456` |
| 服务端口 | `8080` | |
| `JWT_SECRET` | 代码内置默认值 | 生产环境必须通过环境变量覆盖 |
| `MAIL_USERNAME` / `MAIL_PASSWORD` | 无 | SMTP 账号，可选；未配置时仅邮箱验证码功能不可用 |

### 4.2 启动命令

**首次启动**（自动建库建表 + 写入演示数据）：

```bash
cd backend
# Windows
./mvnw.cmd spring-boot:run "-Dspring-boot.run.profiles=dev,init"
# Linux / macOS
./mvnw spring-boot:run "-Dspring-boot.run.profiles=dev,init"
```

**日常启动**（数据已初始化，去掉 `init` 即可）：

```bash
./mvnw spring-boot:run "-Dspring-boot.run.profiles=dev"
```

### 4.3 验证启动成功

- 控制台出现 `Started CampusBackendApplication` 即启动成功
- 业务接口验证：浏览器访问 `http://localhost:8080/api/v2/categories` 返回分类数据
- 接口文档：`http://localhost:8080/doc.html`（Knife4j）
- 健康检查：`http://localhost:8080/actuator/health` —— 未配置 SMTP 时邮件项为 DOWN（整体 503）属正常现象，不影响业务功能

### 4.4 看板演示数据（可选）

「经营数据看板」默认按当前登录卖家的真实商品做 SQL 聚合；若商品较少，想先看丰富效果，可用种子脚本给展示卖家 `user02` 灌入一批覆盖近 30 天、含已售出商品的演示数据（幂等，可重复执行）：

```powershell
# 本地 MySQL
mysql -uroot campus_market_dev < src\main\resources\seed-dashboard-data.sql
```

执行后登录 `user02` / `123456` → 个人中心 → 经营看板，即可见发布/售出/成交额汇总、近 30 天浏览趋势、类目占比、浏览量 Top 商品等图表。

---

## 五、前端启动

### 5.1 安装依赖并启动

```bash
cd frontend
npm install
npm run dev
```

启动后访问：http://localhost:3000

### 5.2 配置说明

开发环境配置文件：`frontend/.env.development`

- 开发环境所有 `/api` 请求经 Vite 代理转发至 `http://localhost:8080`（配置见 `vite.config.ts`），无需额外设置 API 地址
- `VITE_APP_TITLE` 控制页面标题

---

## 六、技术栈概览

### 前端
- **框架**：Vue 3.4（Composition API + `<script setup>`）
- **语言**：TypeScript 5.4（`vue-tsc` 构建期类型检查）
- **构建**：Vite 5（代理 / vendor 分包 / 路由懒加载）
- **样式**：Tailwind CSS 4（`@tailwindcss/vite` + `@theme` 设计令牌）+ CSS 变量设计系统
- **路由**：Vue Router 4（全量懒加载 + 全局守卫白名单鉴权）
- **状态管理**：Pinia（setup Store）+ pinia-plugin-persistedstate 持久化 + 自定义 splitStorage
- **网络**：Axios（拦截器自动携带 JWT / 统一 401 处理）
- **实时通信**：原生 WebSocket 封装（心跳保活 + 指数退避重连）
- **规范**：ESLint + Prettier、DOMPurify 防 XSS

### 后端
- **框架**：Spring Boot 3.3.5（Java 17）
- **ORM**：MyBatis 3.0.3
- **安全**：Spring Security + JWT（jjwt 0.11.5）+ BCrypt
- **数据库**：MySQL 8.0（21 张表）
- **API 文档**：Knife4j（OpenAPI 3）
- **工具库**：Lombok、Jackson、Thumbnailator（图片压缩）

---

## 七、常见问题

### Q1：后端启动报错 "Access denied for user"
> 环境变量 `DB_PASSWORD` 与你的 MySQL 密码不一致。重新设置后需**重开终端**再启动（`setx` 对已开终端不生效）。

### Q2：前端启动后页面空白 / 接口 404
> 确认后端已启动。开发环境请求走 Vite 代理（`/api` → 8080），检查 `vite.config.ts` 中代理配置与后端端口一致。

### Q3：端口被占用
> 后端默认 8080，前端默认 3000。可在 `application-dev.yml` / `vite.config.ts` 中修改端口。

### Q4：Maven 下载依赖慢
> 可在 `~/.m2/settings.xml` 中配置阿里云镜像加速。

### Q5：`/actuator/health` 显示 503
> 未配置 SMTP（`MAIL_USERNAME` / `MAIL_PASSWORD`）时邮件健康检查为 DOWN。仅影响邮箱验证码功能，业务接口正常；如需消除可配置 SMTP 或在 dev 配置中添加 `management.health.mail.enabled: false`。

---

## 八、账户信息

- 演示账号：`user01` / `123456`（另有 user02~user80 及配套商品、帖子、评论数据）
- 数据库：本地 `root`，密码通过环境变量 `DB_PASSWORD` 注入
- JWT 密钥：代码内置开发默认值，生产环境必须通过 `JWT_SECRET` 环境变量覆盖
